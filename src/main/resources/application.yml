spring:
  application:
    name: hms-backend

  datasource:
    url: jdbc:mysql://localhost:3306/hms_db
    username: root
    password: Root123@#$
    driver-class-name: com.mysql.cj.jdbc.Driver

  jpa:
    hibernate:
      ddl-auto: update
    show-sql: true
    format_sql: true

server:
  port: 9090

logging:
  level:
    root: INFO
    com.hms: DEBUG